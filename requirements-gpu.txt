# Install the CUDA-enabled Torch build appropriate for your NVIDIA driver first:
# https://pytorch.org/get-started/locally/
# Minimal dependencies for train_gpu.py, benchmark_gpu.py, and the test suite.
torch>=2.2
numpy>=1.24,<2
PyYAML>=6.0
Pillow>=10.0
pytest>=7.0
