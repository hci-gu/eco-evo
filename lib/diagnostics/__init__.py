"""Diagnostic rigs that measure the ecosystem itself, not a policy.

Everything in here runs the reference NumPy tick with *frozen* behaviour
(no ARS, no learning) so the result is a statement about parameters and
mechanisms rather than about training.
"""
